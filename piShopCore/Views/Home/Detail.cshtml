@model Pie
@{
    ViewData["Title"] = "Pie Detail";
}

<div class="card">
    <div class="container-fluid">
        <img alt="@Model.Name" src="@Model.ImgThumbnaiUrl"/>
    </div>

    <div class="card-body">
        <div class="card-title">
            <h3 class="float-right">@Model.Price.ToString("c")</h3>
            <h3>
                <a href="#">
                    @Model.Name
                </a>
            </h3>
        </div>
          
          <p class=""> @Model.LongDescription </p>
        </div>
    </div>    

